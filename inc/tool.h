/* some small functions to do the job well. */
#ifndef _TOOL_H
#define _TOOL_H


/* swap the value of two numbes */
void swap(int *first, int *second);

int  less(int a, int b);

int  greter(int a, int b);

#endif
